import crypto from 'node:crypto';
type V = string | number | boolean | null;
const DEFAULT_DB='libsql://rehaflow-echomedtechnologies.aws-ap-south-1.turso.io';
const sha=(s:string)=>crypto.createHash('sha256').update(s).digest('hex');
const uid=(p:string)=>`${p}_${crypto.randomBytes(8).toString('hex')}`;
const arg=(v:V)=>v==null?{type:'null'}:typeof v==='boolean'?{type:'integer',value:v?'1':'0'}:typeof v==='number'?{type:Number.isInteger(v)?'integer':'float',value:String(v)}:{type:'text',value:String(v)};
class Turso{url:string;token:string;constructor(){let base=(process.env.TURSO_DATABASE_URL||process.env.DATABASE_URL||DEFAULT_DB).trim().split('?')[0].replace(/\/+$/,'');if(base.startsWith('libsql://'))base=base.replace('libsql://','https://');this.url=`${base}/v2/pipeline`;this.token=(process.env.TURSO_AUTH_TOKEN||'').trim();if(!this.token)throw new Error('TURSO_AUTH_TOKEN is not configured')}async run(sql:string,values:V[]=[]){const r=await fetch(this.url,{method:'POST',headers:{Authorization:`Bearer ${this.token}`,'Content-Type':'application/json'},body:JSON.stringify({requests:[{type:'execute',stmt:{sql,...(values.length?{args:values.map(arg)}:{})}},{type:'close'}]})});const p:any=await r.json().catch(()=>({}));if(!r.ok)throw new Error(`Turso HTTP ${r.status}: ${p?.error?.message||r.statusText}`);for(const z of p.results||[])if(z?.type==='error')throw new Error(`Turso SQL error: ${z.error?.message||z.error}`);const result:any=(p.results||[]).find((x:any)=>x?.response?.type==='execute')?.response?.result||{};const cols=result.cols||result.columns||[];const rows=(result.rows||[]).map((rr:any[])=>{const o:any={};cols.forEach((c:any,i:number)=>{const n=typeof c==='string'?c:c.name;const v=rr[i];o[n]=v&&typeof v==='object'&&'value'in v?v.value:v&&typeof v==='object'&&'base64'in v?Buffer.from(v.base64,'base64'):v});return o});return{rows,affected:Number(result.affected_row_count||0)}}}
let dbi:Turso|null=null;const db=()=>dbi||(dbi=new Turso());let ready:Promise<void>|null=null;
const roleSeeds:[string,string][]=[['admin','Адміністратор'],['manager','Керівник'],['doctor','Лікар'],['nurse','Медсестра'],['registrar','Реєстратура']];
const permissionNames:Record<string,string>={'dashboard.view':'Перегляд оперативного центру','patients.view':'Перегляд пацієнтів','patients.create':'Створення пацієнтів','patients.edit':'Редагування пацієнтів','patients.discharge':'Виписка пацієнтів','rehab.view':'Перегляд реабілітації','rehab.manage':'Керування реабілітацією','rooms.view':'Перегляд палат','rooms.manage':'Керування палатами','beds.view':'Перегляд ліжок','beds.manage':'Керування ліжками','appointments.view':'Перегляд записів','appointments.manage':'Керування записами','tasks.view':'Перегляд завдань','tasks.manage':'Керування завданнями','treatment.view':'Перегляд лікування','treatment.manage':'Керування лікуванням','documents.view':'Перегляд документів','documents.manage':'Керування документами','users.view':'Перегляд персоналу','users.manage':'Керування персоналом','roles.view':'Перегляд ролей','roles.manage':'Керування ролями','sessions.view':'Перегляд сесій','sessions.revoke':'Завершення сесій','audit.view':'Перегляд аудиту','reports.view':'Перегляд звітів','dashboard':'dashboard alias','patients':'patients alias','archive':'archive alias','reception':'reception alias','beds':'beds alias','admin':'admin alias','rehab':'rehab alias','*':'Повний доступ'};
const matrix:Record<string,string[]>={admin:Object.keys(permissionNames),manager:['dashboard.view','patients.view','patients.create','patients.edit','patients.discharge','rehab.view','rehab.manage','rooms.view','rooms.manage','beds.view','beds.manage','appointments.view','appointments.manage','tasks.view','tasks.manage','treatment.view','treatment.manage','documents.view','documents.manage','users.view','users.manage','roles.view','sessions.view','sessions.revoke','audit.view','reports.view','dashboard','patients','archive','reception','beds','admin','rehab'],doctor:['dashboard.view','patients.view','patients.create','patients.edit','patients.discharge','rehab.view','rehab.manage','rooms.view','beds.view','appointments.view','appointments.manage','tasks.view','tasks.manage','treatment.view','treatment.manage','documents.view','documents.manage','dashboard','patients','archive','reception','beds','rehab'],nurse:['dashboard.view','patients.view','rehab.view','rooms.view','beds.view','appointments.view','tasks.view','tasks.manage','treatment.view','documents.view','documents.manage','dashboard','patients','archive','beds','rehab'],registrar:['dashboard.view','patients.view','patients.create','patients.edit','rooms.view','beds.view','appointments.view','appointments.manage','documents.view','documents.manage','dashboard','patients','archive','reception']};
export async function ensureRbac(){if(ready)return ready;ready=(async()=>{await db().run(`CREATE TABLE IF NOT EXISTS rf_roles(id TEXT PRIMARY KEY,key TEXT UNIQUE NOT NULL,name TEXT NOT NULL,active INTEGER DEFAULT 1,created_at TEXT DEFAULT CURRENT_TIMESTAMP,updated_at TEXT DEFAULT CURRENT_TIMESTAMP)`);await db().run(`CREATE TABLE IF NOT EXISTS rf_permissions(id TEXT PRIMARY KEY,key TEXT UNIQUE NOT NULL,name TEXT NOT NULL,active INTEGER DEFAULT 1,created_at TEXT DEFAULT CURRENT_TIMESTAMP)`);await db().run(`CREATE TABLE IF NOT EXISTS rf_role_permissions(role_id TEXT NOT NULL,permission_id TEXT NOT NULL,PRIMARY KEY(role_id,permission_id))`);for(const [key,name]of roleSeeds){const r=await db().run('SELECT id FROM rf_roles WHERE key=? LIMIT 1',[key]);if(!r.rows.length)await db().run('INSERT INTO rf_roles(id,key,name,active) VALUES(?,?,?,1)',[uid('role'),key,name])}for(const [key,name]of Object.entries(permissionNames)){const p=await db().run('SELECT id FROM rf_permissions WHERE key=? LIMIT 1',[key]);if(!p.rows.length)await db().run('INSERT INTO rf_permissions(id,key,name,active) VALUES(?,?,?,1)',[uid('perm'),key,name])}for(const [role,keys]of Object.entries(matrix)){const rr=await db().run('SELECT id FROM rf_roles WHERE key=?',[role]);for(const key of keys){const pp=await db().run('SELECT id FROM rf_permissions WHERE key=?',[key]);if(rr.rows[0]&&pp.rows[0])await db().run('INSERT OR IGNORE INTO rf_role_permissions(role_id,permission_id) VALUES(?,?)',[rr.rows[0].id,pp.rows[0].id])}}})().catch(e=>{ready=null;throw e});return ready}
export async function roles(){await ensureRbac();const rows=(await db().run(`SELECT r.key,r.name,r.active,COALESCE((SELECT json_group_array(p.key) FROM rf_role_permissions rp JOIN rf_permissions p ON p.id=rp.permission_id WHERE rp.role_id=r.id AND p.active=1),'[]') permissions FROM rf_roles r WHERE r.active=1 ORDER BY r.key`)).rows;return rows.map((r:any)=>({...r,active:Number(r.active),permissions:typeof r.permissions==='string'?JSON.parse(r.permissions):r.permissions}))}
export async function current(accessToken:string){if(!accessToken)return null;await ensureRbac();const r=await db().run(`SELECT u.id,u.email,u.name,u.role,u.active,s.id session_id,s.expiresAt FROM Session s JOIN User u ON u.id=s.userId WHERE s.tokenHash=? LIMIT 1`,[sha(accessToken)]);const u:any=r.rows[0];if(!u||Number(u.active)!==1||new Date(u.expiresAt).getTime()<Date.now())return null;const ps=(await db().run(`SELECT p.key FROM rf_role_permissions rp JOIN rf_permissions p ON p.id=rp.permission_id JOIN rf_roles r ON r.id=rp.role_id WHERE r.key=? AND r.active=1 AND p.active=1`,[u.role==='reception'?'registrar':u.role])).rows;return {...u,permissions:ps.map((x:any)=>x.key)}}
export const allowed=(user:any,permission:string)=>Array.isArray(user?.permissions)&&(user.permissions.includes('*')||user.permissions.includes(permission));
export function permissionForPath(path:string,method:string){if(method==='OPTIONS'||path==='/login'||path==='/logout'||path==='/me'||path==='/session'||path==='/auth/login'||path==='/auth/logout'||path==='/auth/me')return null;if(path==='/roles')return'roles.view';if(path.startsWith('/dashboard'))return'dashboard.view';if(path.startsWith('/patients/'))return method==='DELETE'||path.endsWith('/archive')?'patients.discharge':method==='GET'?'patients.view':'patients.edit';if(path==='/patients')return method==='GET'?'patients.view':'patients.create';if(path.startsWith('/archive')||path.startsWith('/patient-history'))return'archive';if(path.startsWith('/rooms'))return method==='GET'?'rooms.view':'rooms.manage';if(path.startsWith('/beds'))return method==='GET'?'beds.view':'beds.manage';if(path.startsWith('/tasks'))return method==='GET'?'tasks.view':'tasks.manage';if(path.startsWith('/treatment-records'))return method==='GET'?'treatment.view':'treatment.manage';if(path.startsWith('/appointments'))return method==='GET'?'appointments.view':'appointments.manage';if(path.startsWith('/prescriptions'))return method==='GET'?'treatment.view':'treatment.manage';if(path.startsWith('/documents'))return method==='GET'?'documents.view':'documents.manage';if(path.startsWith('/users')||path.startsWith('/staff'))return method==='GET'?'users.view':'users.manage';if(path.startsWith('/sessions'))return method==='GET'?'sessions.view':'sessions.revoke';if(path.startsWith('/doctors'))return'users.view';if(path.startsWith('/audit'))return'audit.view';if(path.startsWith('/reports'))return'reports.view';return null}
export async function listRoles(){return roles()}
